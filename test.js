let num = 4
let str = 'hello'
const value = true && false ? /value/ : value()
call()
call.member();
(a || b)()
for (const prop of object) {
    for (let key in object) {
        object[key]
    }
}
class Test extends Component {
    constructor(props) {
        super(props)
    }
}
